const Footer = () => {
  return (
    <footer className="border-t border-border py-8 text-center text-sm text-text-muted">
      © {new Date().getFullYear()} Pixora — Capture Moments, Cherish Forever.
    </footer>
  );
};

export default Footer;